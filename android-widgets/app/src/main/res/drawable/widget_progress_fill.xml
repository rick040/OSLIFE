<?xml version="1.0" encoding="utf-8"?>
<!-- Progress fill for the active-projects widget's per-project ProgressBar (android:progressDrawable). Clip layer is required so android:progress actually reveals a fraction of the fill. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <solid android:color="@color/widget_divider" />
            <corners android:radius="4dp" />
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <solid android:color="@color/widget_accent_projects" />
                <corners android:radius="4dp" />
            </shape>
        </clip>
    </item>
</layer-list>
