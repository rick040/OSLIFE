<?xml version="1.0" encoding="utf-8"?>
<!-- Used by the floating quick-capture dialog (activity_quick_add.xml) only —
     that dialog keeps its own light-card design language, separate from the
     dark-glass home-screen widget cards. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/widget_accent_braindump" />
    <corners android:radius="12dp" />
</shape>
